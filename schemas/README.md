# Schemas

This directory will contain stable JSON schemas for AgentScope scan outputs.

The schema should be product-neutral: Codex, Claude Code, Cursor, and generic
agent workspaces should all map into the same model.
